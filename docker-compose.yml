version: '3.3'

services:
  docker-node:
    build: ./node
    image: docker-node:local
    container_name: node-local
    volumes:
      - ./node:/node
      - /node/node_modules
    ports:
      - 3000:3000
    environment:
      - NODE_ENV=development
  docker-nginx:
    build: ./nginx
    image: docker-nginx:local
    container_name: nginx-local
    ports:
      - 8081:80
    links:
      - docker-node # link "docker-nginx" to "docker-node"
    environment:
      - NGINX_PORT=80
    volumes:
      - ./nginx/html:/usr/share/nginx/html
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf